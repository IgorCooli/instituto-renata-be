spring:
  application:
    name: instituto-renata-api
  profiles:
    # Preferir SPRING_PROFILES_ACTIVE em CI/testes; caso contrário ENV (local | staging | production).
    active: ${SPRING_PROFILES_ACTIVE:${ENV:local}}
  jpa:
    open-in-view: false

server:
  port: ${HTTP_PORT:8080}
